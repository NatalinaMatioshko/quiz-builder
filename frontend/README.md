# Frontend

Next.js + TypeScript + Tailwind CSS client for Quiz Builder.

## Setup

```bash
npm install
npm run dev
```

## Environment

Create `.env.local`:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
```

## Routes

- `/`
- `/create`
- `/quizzes`
- `/quizzes/:id`
